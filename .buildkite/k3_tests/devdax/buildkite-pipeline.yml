# Paste into a dedicated manual/nightly pipeline's Steps editor.
agents:
  queue: k3-h200-local
steps:
  - label: ":pipeline: Select DevDAX coverage"
    command: bash .buildkite/k3_tests/devdax/upload.sh
